import React from 'react';
import { View, Text } from 'react-native';

const CartScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 24, fontWeight: 'bold', margin: 10 }}>Your Cart</Text>
            {/* Cart Items List */}
        </View>
    );
};

export default CartScreen;
